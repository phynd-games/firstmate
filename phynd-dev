#!/usr/bin/env bash
# Repository entrypoint for the phynd-dev implementation.
set -euo pipefail

ROOT="$(CDPATH='' cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec "$ROOT/bin/phynd-dev" "$@"
