# Test-phase acceptance ledger
- [x] Preserve the ordinary two-parent merge and unchanged CI tree (git object/ancestry comparison).
- [x] Launch records and executable spawn/control/teardown reject uncertain duplicates and preserve exact identities (focused launch suites).
- [x] Supervisor quarantine requires every disappearance fact, with negative controls and recovery (supervisor suite alone).
- [x] Server launch environment isolation and per-caller wake containment work (adapter and pending-reply cases).
- [x] Helper and document-reader launches retain intent, readiness, and cleanup (helper and reader suites).
- [x] Native facts exercised only in the named isolated lab, or missing capability reported.
- [x] Collect actual CLI/state/HTTP evidence and remove transient worktree files.
Lint, publication, PR, CI, merge, and adoption belong to the outer executor and are not run here.

All scoped checks completed. The two known suite failures were reproduced against base production code; the adapter baseline used only the four disclosed terminal-id fixture corrections. The original reader run skipped without runtime discovery; the focused launch-recovery retry passed with the private interpreter supplied. The timeout-contract selector uses a stand-in ShellCheck and executes no static analysis. The native lab passed on Herdr 0.8.2 and cleaned up. Chrome wrapper capture produced no file; direct isolated headless Chrome captured the actual page, and the browser and reader were stopped. No source changes were needed.
